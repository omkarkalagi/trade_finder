{
  "name": "trade-dashboard",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "prebuild": "eslint src --ext .ts,.tsx --config eslint.config.js --ignore-pattern 'build/*' --quiet",
    "verify": "node verify-build.mjs",
    "preview": "vite preview",
    "start": "vite preview --port 3000",
    "lint": "eslint src --ext .ts,.tsx --config eslint.config.js",
    "deploy": "npm run build && npx vercel --prod"
  },
  "dependencies": {
    // ... your existing dependencies
  },
  "devDependencies": {
    // ... your existing devDependencies
    "@typescript-eslint/eslint-plugin": "^7.8.0",
    "@typescript-eslint/parser": "^7.8.0"
  }
}
